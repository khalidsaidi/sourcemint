# .ai (internal agent workspace)

This directory is reserved for AI-agent internal artifacts (logs, scratch, intermediate files).
It is intentionally gitignored (except this README) so it won't pollute the application code.
